Load `.agents/skills/remanufacturing-data-vault-playbook/SKILL.md` and use it as the operating playbook for this task.

Model these new seed sources into a coherent remanufacturing Data Vault slice:
- `raw_reman_cores`
- `raw_reman_work_orders`
- `raw_reman_inspections`
- `raw_reman_part_transactions`
- `raw_reman_shipments`

Assume these are early landed raw source extracts for Caterpillar remanufacturing operations.

Please:
- inspect the seed files first
- determine the correct business keys and grain
- create `stg_<subject>` models
- create `stg_vault_<subject>` hashed staging
- create hubs, links, and satellites in the existing project structure
- add YAML docs and `data_tests:`
- validate with dbt builds

Design expectations:
- core/component should likely be a hub
- work order should likely be a hub
- part should likely be a hub where appropriate
- inspections, part usage, and shipment/state details should likely live in satellites
- use links for core-to-work-order and other natural business relationships
- only create a business-facing mart if there is a clear consumer-facing output worth exposing

Package expectations:
- use `automate_dv` if dbt runtime supports it
- if Studio/editor shows `automate_dv` errors, verify with `dbt compile --select <model> --no-partial-parse` before deciding there is a real problem